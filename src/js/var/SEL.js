/**
 * Selector constants
 * @const
 */
define([
    "./CLASS"
], function(CLASS) {
    return {
        ROOT: '.' + CLASS.ROOT,
        NODE: '.' + CLASS.NODE,
        TRANSITION: '.' + CLASS.TRANSITION
    };
});